# 2026-05-22 工作记录

## opponent-prediction-strategy 变更完成

### 已完成
- 四件套：proposal.md / design.md / tasks.md / bugfix.md
- 代码修改：
  - `game/stable_strategy_model.py`：StrategyModelContext 增加 opponent_prediction；评分增加危险牌惩罚和听牌概率加权；_reasons 增加 `[预测]` 标记
  - `game/stable_hard_analysis.py`：将 opponent_prediction 传入 StrategyModelContext
  - `ui/stable_battle_panel.py`：advice_reason 中 `[预测]` 和对手预测文本用红色高亮
- 回归测试：TC1/TC2/TC3 全部通过
- gitnexus detect-changes：风险等级 critical（预期内，因修改了核心策略模型）

### 关键设计
- 对手预测关闭时行为完全一致（兼容性）
- 危险牌 high/medium/low 分别额外扣 80/35/12 分
- 听牌概率越高，danger 权重越大（×1.0 ~ ×1.8）
- 等待牌按概率额外扣分（最高约60分）
- UI 通过 `[预测]` 前缀标记识别红色高亮文本

## opponent-prediction-strategy 追加：候选重排预测扣分列

### 追加修改
- `game/stable_hard_analysis.py`：HardDiscardCandidate 增加 `opponent_penalty: float = 0.0`
- `game/stable_strategy_model.py`：StrategyModelScore 增加 `opponent_penalty`；rank_discard_candidates 将 penalty 写入 candidate
- `ui/stable_battle_panel.py`：`_format_candidates_html` 中，当 `opponent_penalty > 0` 时显示红色 `预测扣 X.X`

### 效果
候选重排现在显示：
1. 打 6万 | 分 -838.7 | 向听 1 | 进张 38 | **预测扣 35.0（红色）**
2. 打 8万 | 分 -836.0 | 向听 1 | 进张 35

预测扣分为0时不显示，保持界面简洁。

## opponent-prediction-strategy 追加：始终显示预测扣分

### 修改
- `ui/stable_battle_panel.py`：`_format_candidates_html` 中，无论 `opponent_penalty` 是否为0都显示 `预测扣 X.X`
- 扣分为0时用灰色（#8e8e93），扣分为正时用红色（#e74c3c）

### 效果
候选重排统一显示预测扣分列：
1. 打 9万 | 分 303.0 | 向听 0 | 进张 12 | **预测扣 0.0（灰色）**
2. 打 4条 | 分 -23.0 | 向听 0 | 进张 4 | **预测扣 12.0（红色）**
