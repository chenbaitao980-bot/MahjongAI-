# 2026-05-22 工作日志

## stable-panel-layout-optimization 布局优化
- 修复 `ui/stable_battle_panel.py` 右侧策略建议区域布局问题
- `_hand_structure_edit`: FixedHeight(85) -> MinimumHeight(60) + QSizePolicy.MinimumExpanding，多组合时自动撑开
- `_hard_calc_edit`: MinimumHeight(600) -> MinimumHeight(400)，配合两列布局更高效利用空间
- `_format_strategy_analysis_html()`: 硬算明细改为两列布局（左列状态类/右列建议类），候选重排全宽展示
- `_format_hand_structure_arrangements_html()`: 添加 line-height 和撑开元素确保容器自适应
- 通过 `python -c "import ui.stable_battle_panel"` 语法检查
- GitNexus detect-changes 通过，确认只影响预期符号
- OpenSpec 四件套完整：proposal/design/specs/tasks + regression-tests/cases

## stable-panel-layout-optimization 修复（第二轮）
- 问题：两列布局未生效，QTextEdit 不支持 CSS display:flex
- 修复：将 flex 布局改为 HTML table 布局（`<table><tr><td>左列</td><td>右列</td></tr></table>`）
- 经验：Qt 的 QTextEdit HTML 渲染引擎对 CSS3 flex 支持有限，table 布局兼容性最好

## stable-panel-layout-optimization 用户验收通过
- 手牌结构区域自适应高度：用户确认多组合时不截断
- 硬算明细两列布局：用户确认正常显示
- 所有验收项已完成，change 状态更新为完成
