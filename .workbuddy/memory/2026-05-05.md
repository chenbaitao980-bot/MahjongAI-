# 2026-05-05 工作记录

## 手牌区副露溢入修复
- 问题：手牌区把副露区的八万也切进来了，因为 `_detect_self_meld_count_visual` 有时误判 meld_count=0，导致 `hand_region(0)` 返回全宽区域
- 修复：在 `pipeline.py` 的 `segment_tiles_with_slots` 返回后插入 Y 方向高度校验后处理
  - 取所有 ROI 高度中位数作为基准，阈值 0.75
  - 从左侧逐个检查，高度低于阈值的视为副露溢入并删除
  - 仅 meld_side=left 时生效
  - 文件：`vision/pipeline.py` 第240-264行

## HandRegionModule 缺失方法修复
- 问题：`HandRegionModule._segment_tiles()` 调用 `self._segment_tiles_by_white_components()` 但方法不存在，报 AttributeError
- 根因：从 `RecognitionPipeline` 重构提取 `HandRegionModule` 时，该方法被遗漏未搬运
- 修复：从 `pipeline.py:1511-1577` 复制方法到 `hand_region_module.py`，调整签名匹配调用处 4 参数 `(strip, expected_count, frame_index, debug_dir)`
- 文件：`vision/hand_region_module.py` 第559-625行
